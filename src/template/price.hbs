<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Date and Price Table</title>
    <style>
        body {
            font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
            background-color: #080808;
            margin: 0;
            padding: 0;
        }

        table {
            margin: 10px auto;
            border-collapse: collapse;
        }

        th, td {
            padding: 5px 12px;
            text-align: center;
            font-size: 10vw;
            color: #F7F7F7;
        }

        .lastUpdateTime {
            font-size: 5vw;
            color: #C7C7C7;
            text-align: center;
        }

        @media screen and (min-width: 1200px) {
            th, td {
                font-size: 120px;
            }
            .lastUpdateTime {
                font-size: 60px;
            }
        }
    </style>
</head>
<body>
<table>
    <thead>
    <tr>
        <th></th>
        <th>⚡️</th>
        <th>🔥</th>
    </tr>
    </thead>
    <tbody>
    {{#each prices}}
        <tr>
            <td>{{this.date}}</td>
            <td>{{this.electricityPrice}}</td>
            <td>{{this.gasPrice}}</td>
        </tr>
    {{/each}}
    </tbody>
</table>
<div class="lastUpdateTime">{{lastUpdateTime}}</div>
</body>
</html>
