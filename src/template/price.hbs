<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Tracker Price - {{regionName}}</title>
    <style>
        body {
            font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
            background-color: #080808;
            margin: 0;
            padding: 0;
        }
        table {
            margin: 10px auto;
            border-collapse: collapse;
        }
        th, td {
            padding: 0.4vw 3vw;
            text-align: center;
            font-size: 8vw;
            color: #F7F7F7;
        }
        .last-update {
            font-size: 6vw;
            color: #878787;
            text-align: center;
        }
        .footer {
            font-size: 4vw;
            color: #878787;
            text-align: center;
        }
        .menu-toggle {
            position: absolute;
            padding: 0 2vw;
            background: none;
            border: none;
            color: #AAAAAA;
            font-size: 5vw;
            cursor: pointer;
            z-index: 200;
        }
        .menu-container {
            position: absolute;
            top: 0;
            width: 50%;
            min-height: 100%;
            background-color: #222222;
            padding: 8vw 2vw 1vw;
            box-sizing: border-box;
            z-index: 100;
            transition: left 0.3s ease;
            left: -50%;
            color: #F7F7F7;
            font-size: 3vw;
        }
        .menu-container.open {
            left: 0;
        }
        .menu-section {
            font-weight: bold;
            border-bottom: #878787 0.5vw solid;
            padding: 1vw 1vw;
        }
        .menu {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }
        .menu li {
            padding: 1vw 2vw;
        }
        .menu li.selected {
            background: #444444;
        }
        .menu a {
            color: #F7F7F7;
            text-decoration: none;
        }

        @media screen and (min-width: 1200px) {
            th, td {
                padding: 4.8px 36px;
                font-size: 96px;
            }
            .last-update {
                font-size: 72px;
            }
            .footer {
                font-size: 48px;
            }
            .menu-toggle {
                font-size: 60px;
                padding: 0 24px;
            }
            .menu-container {
                font-size: 36px;
                width: 600px;
                left: -600px;
                padding: 96px 24px 12px;
            }
            .menu-section {
                padding: 12px 12px;
                border-bottom: #878787 6px solid;
            }
            .menu li {
                padding: 12px 24px;
            }
        }
    </style>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const menuToggle = document.querySelector('.menu-toggle');
            const menu = document.querySelector('.menu-container');

            menuToggle.addEventListener('click', function() {
                menu.classList.toggle('open');
            });
        });
    </script>
</head>
<body>
<button class="menu-toggle">&#9776;</button>
<div class="menu-container">
    <div class="menu-section">Tracker Tariff</div>
    <ul class="menu">
        <li class="selected">{{productName}}</li>
        {{#each otherProducts}}
            <li><a href="{{this.link}}">{{this.name}}</a></li>
        {{/each}}
    </ul>
    <div class="menu-section">Region</div>
    <ul class="menu">
        <li class="selected">{{regionName}}</li>
        {{#each otherRegions}}
            <li><a href="{{this.link}}">{{this.name}}</a></li>
        {{/each}}
    </ul>
    <div class="menu-section">Useful Links</div>
    <ul class="menu">
        <li><a href="https://www.nationalgrid.co.uk/our-network/distribution-area-search">Find my region</a></li>
        <li><a href="https://play.google.com/store/apps/details?id=com.binarysmith.webclipwidget.ad&hl=en&gl=US">Create a widget in Android</a></li>
        <li><a href="https://apps.apple.com/us/app/glimpse-2/id1524217845">Create a widget in iPhone</a></li>
    </ul>
</div>

<table>
    <thead>
    <tr>
        <th></th>
        <th>⚡️</th>
        <th>🔥</th>
    </tr>
    </thead>
    <tbody>
    {{#each prices}}
        <tr>
            <td>{{this.date}}</td>
            <td>
                {{#if this.electricityPrice}}
                    {{this.electricityPrice}}p
                {{/if}}
            </td>
            <td>
                {{#if this.gasPrice}}
                    {{this.gasPrice}}p
                {{/if}}
            </td>
        </tr>
    {{/each}}
    </tbody>
</table>
<div class="last-update">{{lastUpdateTime}}</div>
<div class="footer">{{regionName}}</div>
<div class="footer">{{productName}}</div>
</body>
</html>
